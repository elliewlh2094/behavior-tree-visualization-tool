# 使用手冊

這份文件是 BT Visualizer 的使用手冊，這裡假設你已經知道什麼是行為樹，並且已經開啟了應用程式。

> 本文件會隨著功能發布而更新。Last updated: 2026.06.28。

## 鍵盤快捷鍵參考

| 快捷鍵 | 動作 |
| --- | --- |
| Shift + Click | 加入或移出目前選取項目 |
| Shift + Drag | 框選節點（節點的邊必須用點選選取） |
| Ctrl / Cmd + A | 選取所有節點與邊 |
| Delete / Backspace | 移除所有選取的節點與邊（一次操作） |
| Ctrl / Cmd + D | 原地複製選取物件|
| Ctrl / Cmd + Z | 復原上一個操作 |
| Ctrl / Cmd + Shift + Z | 重做上一個復原 |
| Ctrl / Cmd + S | 將當前的樹儲存為 JSON 檔案 |
| Ctrl / Cmd + O | 從 JSON 檔案開啟樹 |
| Ctrl / Cmd + F | 在目前的樹中依名稱搜尋節點 |

## 常見工作流程

### 從範本開始

登陸頁有一個 **「Or start from a template」** 區塊。點擊 **Chase** 卡片，即可開啟預先載入該樹的編輯器。

>這是一個簡易的機器人行為樹範例：追擊看得見的目標，否則退回 **Patrol** 子樹（以 `SubTree` 參照的方式內含，因此，這也是一個簡易的多樹範例）。

### 建立一棵樹

從登陸頁的 **「Go to Editor」** 按鈕點擊前往，一律會開啟一棵全新的空白樹（只包含一個 Root 根節點）。

1. 從左側節點面板可以選擇並拖曳一種節點類型到畫布上。
2. 點選節點底部把手，可以拖曳連線到子節點頂部把手，以建立節點連線。Action 與 Condition 節點是葉節點，設計上沒有底部把手。
3. 點擊節點，即可在右側屬性面板中編輯其 **名稱（name）** 與 **節點類型（kind）**。Root 節點的節點類型鎖定不可編輯，但 name 可以編輯。

### 選取多個項目

- `Shift + click` 第二個節點，將它加入選取項目。
- 在空白畫布上 `Shift + drag`，一次框選多個節點。框選只會涵蓋節點；邊必須透過點擊選取。
- `Ctrl/Cmd+A` 會選取所有項目。

選取多個項目時，屬性面板會改為顯示選取節點之摘要，例如 `2 nodes, 1 edge selected`。

### 刪除（Delete）

選取一個或多個項目後，按下 `Delete` 或 `Backspace`。

- **Root 不能被刪除。** 
- 刪除父節點時，其子節點會變成孤立節點（沒有父節點）。它們仍會留在畫布上，只是中斷連線，此時驗證合法性時，驗證面板會將它們標示為 R8 警告。

### 復原與重做（Undo and redo）

Ctrl/Cmd+Z 一律會復原最近一次操作，Ctrl / Cmd + Shift + Z 則是重做上一個復原。歷史紀錄是**最多保留 10 個步驟**，並且在所有子樹之間共用。

### 儲存與開啟檔案（Save and open）

- **儲存（Save）** 會將目前的樹下載到預設的下載位置。格式意義請參照 [`bt-json-format.md`](./docs/bt-json-format.md)。
- **開啟（Open）** 會取代目前的樹。
- 如果開啟的檔案格式錯誤或未通過驗證，工具列會顯示錯誤，且目前的樹會被保留。

### 未存變更保護（Unsaved-changes guard）

當編輯器有**未儲存的編輯**時：

- 瀏覽器分頁標題與檔名欄位皆會顯示一個 **●** 圓點，代表當前文件有未儲存的變更。直到當你 **儲存（Save，Ctrl/Cmd+S）**，或透過 **開啟（Open）** 載入新文件時，標記就會清除。
- **離開編輯器**、**關閉** 或 **重新整理分頁** 時，編輯器會跳出確認對話框，避免你遺失先前的工作成果。選擇取消會讓你留在畫布上，編輯內容完整保留。

### 匯出圖片（Export image）

工具列中的 **Export** 按鈕（位於工具列最右側）會將目前檢視的樹儲存為 PNG 圖片。可選擇：

- **背景：** **Themed background**（目前畫布的顏色，淺色或深色主題）或 **Transparent**（透明背景）。
- **檔名：** 預先填入 `<樹名稱>.png`。

### 多棵樹（Multiple Trees）

一份文件可以包含多棵行為樹。畫布上方的標籤頁會為每棵樹顯示一個分頁。**Main** 是文件的進入點，不能被刪除（可以重新命名）。

- **建立新樹：** 點擊分頁列最右側 **+** 按鈕建立新樹。新樹會自動命名為 `Tree 2`、`Tree 3`，以此類推。
- **重新命名樹：** 雙擊分頁名稱可重新命名樹。重新命名樹也會更新所有參照其舊名稱的 `SubTree` 節點。
- **重新排序分頁：** 將分頁向左右拖曳，放到想要的位置即可。
<!-- 
NOTE: 不主動提及的功能
也可以用鍵盤排序 — 以 `Tab` 聚焦某個分頁，按 `Space` 拿起，用方向鍵移動，再按 `Space` 放下。重新排序為單一復原步驟。
 -->
- **刪除樹：** 將滑鼠懸停在非 Main 分頁上，右側會出現 **×** 按鈕可刪除樹。刪除樹後，指向該樹的 SubTree 節點會變成無效參照，並在驗證或儲存時以顯示錯誤。

### 子樹（Subtrees）

**SubTree** 節點會以參照方式嵌入另一棵樹。選取它即可在屬性面板中操作：

- **樹參照（Tree Reference）：** 從下拉選單選擇 SubTree 指向哪一棵樹（不含目前這棵樹）。若參照的樹已不存在，會以警告標示。
- **名稱為唯讀。** SubTree 的名稱永遠會對應其參照樹的名稱，因此無法在此直接編輯。若要重新命名，請雙擊參照樹的**分頁** — SubTree 節點會隨之更新。若尚未設定有效參照，名稱會顯示 `(no reference)`。
- **Open subtree ↗：** 點擊即可跳到參照樹的分頁。當參照未設定或指向不存在的樹時，此按鈕會被停用。

### 在樹之間移動或複製節點（Move or copy nodes）

選取一個或多個節點，然後點擊工具列的 **Move / Copy**，即可將它們轉移到另一棵樹。

>只有在已選取節點且文件至少有兩棵樹時，此按鈕才會啟用。

在對話框中：

- **目標樹（Destination tree）：** 目前這棵樹以外的任一棵樹，依分頁順序列出。
- **Move** 會將節點從目前的樹移除並加入目標樹，且保留其 ID。**Copy** 會保留原節點，並在目標樹加入全新的複本。
- 摘要會顯示有多少節點與邊會被轉移。連接已選取節點與未選取節點的邊（**邊界邊，boundary edge**）會被捨棄 — 並顯示其數量。
- 確認後，目標分頁會成為作用中分頁，並選取轉移後的節點。一次 `Ctrl/Cmd+Z` 即可跨兩棵樹復原整個操作。

當操作會造成以下情況時，會被封鎖（並顯示行內訊息）：

- 將 **Root** 移動或複製到已經有 Root 的樹，或
- 移動或複製**參照目標樹的 SubTree**，這會造成循環。

### 畫布控制項（Canvas controls）

畫布**左下角**的控制群組包含縮放與符合畫面按鈕，以及一個顯示目前縮放百分比的**縮放比例標籤**。點擊該標籤即可將縮放重設為 **100%**（會保留你的平移位置）。

工具列中的 **Layout** 按鈕會由上而下重新排列目前的樹，並將整棵樹框入畫面。

### 搜尋節點（Search）

在畫布上按 `Ctrl/Cmd+F`，即可在右上角開啟一個浮動搜尋列。

- 輸入關鍵字後，會比對**僅目前這棵樹**的節點名稱（不分大小寫的子字串）。目前不支援跨樹搜尋。
- **Enter** 或 **↓** 跳到下一個相符項；**Shift+Enter** 或 **↑** 跳到上一個。
- **Esc**（或 **×** 按鈕）會關閉搜尋列並清除高亮。

匯出圖片時不會包含搜尋列。

### 驗證（Validate）

點擊工具列中的 **Validate**。底部面板會列出結構問題，錯誤會排在前面。點擊其中一列即可在畫布上選取造成問題的節點，屬性面板也會跳到該節點，方便你直接修正。

驗證是當下狀態的檢查，不會持續自動執行。所以，編輯後請再次執行驗證。

## 理解驗證規則

每項規則的完整說明都記錄在 [`bt-json-format.md` §5](./docs/bt-json-format.md)。

摘要如下：

| ID | 嚴重性 | 意義 | 常見修正方式 |
| --- | --- | --- | --- |
| R1 | error | 必須剛好有一個 Root 節點，且其 id 為 `rootId`。 | 正常 UI 操作不應觸發；若出現，表示檔案異常。 |
| R2 | error | Root 必須剛好有一個子節點。 | 將 Root 連接到單一頂層複合節點。 |
| R3 | error | Action 與 Condition 不能有子節點。 | 移除輸出連線；這些節點是葉節點。 |
| R4 | error | Sequence / Fallback / Parallel 必須有至少 1 個子節點。 | 連接至少一個子節點，或變更節點類型。 |
| R5 | error | Decorator 必須剛好有 1 個子節點。 | 新增或移除子節點，讓數量變成 1。 |
| R6 | error | 樹中不能有循環。 | 刪除回邊以中斷循環。 |
| R7 | error | 非 Root 節點最多只能有 1 個父節點。 | 刪除多餘的輸入連線。 |
| R8 | warning | 非 Root 節點沒有父節點（孤立節點）。 | 將它連接到父節點，或刪除它。 |
