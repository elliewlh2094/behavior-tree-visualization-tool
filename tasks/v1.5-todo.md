# Todo: Behavior Tree Visualization Tool — v1.5 "Repo Hygiene & Documentation"

> Derived from `tasks/roadmap.md` §v1.5. Roadmap is the source of truth; if this drifts, regenerate from the roadmap.
> Legend: `[ ]` not started · `[~]` in progress · `[x]` done · 🛑 = checkpoint (requires human approval to pass)
>
> **Note:** This release has zero application code changes. All tasks are documentation and file organization. No dependencies on other releases — can run in parallel with any feature release if needed.

## Dependency Graph

```
T1 move SPEC.md → docs/SPEC.md + update refs (no deps)
T2 docs/README.md index (no deps)
T3 verify .gitignore (no deps)
    │
    └── T4 capture screenshots (needs app in final state, but no code deps)
            │
            └── T5 README rewrite (needs T1 for correct paths, T4 for screenshots)
```

Implementation order: T1, T2, T3 (parallel) → T4 → T5

---

## Phase 1: Repository Tidy (F16)

### T1 — Move SPEC.md to docs/ + update all references

**Description:** Move `SPEC.md` from the repo root to `docs/SPEC.md`. Update every file that references `SPEC.md` to use the new path. Config files stay at root (tools require them there).

**Acceptance criteria:**
- [ ] `SPEC.md` moved to `docs/SPEC.md` via `git mv`
- [ ] All references updated. Files that reference SPEC.md include (verify with `grep -r`):
  - `README.md`
  - `tasks/*.md` (roadmap, todos)
  - Any ADR files in `docs/adr/`
  - `.claude/` memory files (if applicable)
- [ ] No broken links to SPEC.md after move
- [ ] Optional: move `tasks/` → `docs/tasks/` (decide based on whether it improves clarity)

**Verification:**
- [ ] `grep -r "SPEC.md" --include="*.md"` shows only correct `docs/SPEC.md` paths
- [ ] No dead references

**Dependencies:** None

**Files likely touched:**
- `SPEC.md` → `docs/SPEC.md` (git mv)
- `README.md`
- `tasks/roadmap.md`
- `tasks/v1.1-todo.md` (and other todo files)
- `docs/adr/*.md`

**Estimated scope:** S

---

### T2 — Create docs/README.md as documentation index

**Description:** Create a `docs/README.md` that serves as an index for all project documentation, linking to the spec, ADRs, format docs, and task files.

**Acceptance criteria:**
- [ ] `docs/README.md` exists with:
  - Title: "BT Visualizer Documentation"
  - Links to `SPEC.md` (after T1 move)
  - Links to `bt-json-format.md`
  - Links to `adr/` directory with brief descriptions of each ADR
  - Links to `user-guide.md` if it exists
  - Links to task/roadmap files (if moved to docs/)
- [ ] Clean, scannable format — no prose blocks, just organized links with one-line descriptions

**Verification:**
- [ ] All links resolve to existing files
- [ ] `docs/README.md` renders correctly in GitHub

**Dependencies:** None (but should run after or with T1 for correct paths)

**Files likely touched:**
- `docs/README.md` (new)

**Estimated scope:** XS

---

### T3 — Verify .gitignore coverage

**Description:** Audit `.gitignore` to ensure all generated/transient directories are covered. No functional impact — just housekeeping.

**Acceptance criteria:**
- [ ] `.gitignore` covers at minimum:
  - `node_modules/`
  - `dist/`
  - `coverage/`
  - `playwright-report/`
  - `test-results/`
  - `*.tsbuildinfo`
  - `.DS_Store`
- [ ] No generated files are currently tracked in git (verify with `git ls-files`)
- [ ] Document in the README (T5) why config files live at root: "Build tools (Vite, Tailwind, TypeScript, ESLint, Prettier, Playwright) require their config files at the repository root."

**Verification:**
- [ ] `git status` after a clean build shows no untracked generated files
- [ ] `grep` for each pattern in `.gitignore`

**Dependencies:** None

**Files likely touched:**
- `.gitignore` (if missing entries)

**Estimated scope:** XS

---

## Checkpoint: Tidy Complete 🛑

- [ ] `SPEC.md` lives at `docs/SPEC.md`
- [ ] All internal references updated
- [ ] `docs/README.md` index exists
- [ ] `.gitignore` covers all generated dirs
- [ ] 🛑 **Human review before proceeding to README rewrite**

---

## Phase 2: Documentation (F15)

### T4 — Capture annotated screenshots

**Description:** Capture screenshots of all major UI states and store them in `docs/screenshots/` for use in the README.

**Acceptance criteria:**
- [ ] `docs/screenshots/` directory created (tracked in git)
- [ ] Screenshots captured (PNG, reasonable resolution ~1200px wide):
  - `start-screen.png` — the start screen with "New Tree" and "Open File" buttons
  - `editor-overview.png` — editor with a non-trivial tree (5+ nodes), showing toolbar, palette, canvas, property panel
  - `property-panel.png` — zoomed view of property panel with a node selected (showing parent/children IDs)
  - `validation-panel.png` — validation panel open with at least one error displayed
  - `dark-mode.png` — editor in dark mode (if v1.3 ships before v1.5)
  - `multi-tree.png` — tab bar with multiple trees (if v1.4 ships before v1.5)
- [ ] Screenshots are not annotated with arrows/labels in the image itself — the README text provides context
- [ ] File sizes reasonable (<500KB each, use PNG optimization if needed)

**Verification:**
- [ ] All screenshot files exist and render correctly
- [ ] Screenshots accurately represent the current app state

**Dependencies:** App should be in final v1.4 state for comprehensive screenshots (but can capture incrementally)

**Files likely touched:**
- `docs/screenshots/*.png` (new, 4–6 files)

**Estimated scope:** S (manual capture + optimization)

---

### T5 — README rewrite

**Description:** Expand the README from the current 24-line quickstart to comprehensive project documentation with all sections specified in F15.

**Acceptance criteria:**
- [ ] **Project introduction** (~3–5 sentences):
  - What it is: PWA for authoring and visualizing behavior trees
  - Target audience: robotics/game AI developers, researchers, students
  - Key differentiators: runs entirely in-browser, no server, standards-aligned JSON format
- [ ] **Screenshot**: hero screenshot of the editor (from T4)
- [ ] **Feature list**: all shipped features through the latest release, organized by category:
  - Authoring (8 node kinds, drag-and-drop, snap-to-grid, child ordering)
  - Visualization (per-kind colors/icons, connection handles)
  - Validation (8 structural rules, click-to-select)
  - Persistence (save/load JSON, undo/redo 10 steps)
  - PWA (offline-capable, installable)
  - UI polish (start screen, file rename, design language)
  - Canvas control (grid toggle, auto layout) — if v1.2 shipped
  - Theming (customizable colors, dark mode) — if v1.3 shipped
  - Composition (subtree references, multi-tab) — if v1.4 shipped
- [ ] **Installation**:
  - Prerequisites: Node.js 20+
  - Clone, `npm install`, `npm run dev`
  - All `npm` scripts from SPEC.md §Commands
  - PWA installation note (Chromium "Install app" button)
- [ ] **UI guide**: brief walkthrough with screenshots referenced inline
- [ ] **Architecture overview**: 1-paragraph tech stack (React 18, TypeScript, Tailwind, Zustand, ReactFlow, Vite, PWA) + link to `docs/SPEC.md`
- [ ] **JSON format**: link to `docs/bt-json-format.md` with a brief description
- [ ] **Contributing**: how to run tests, code style (Prettier + ESLint), link to SPEC.md boundaries
- [ ] **License**: reference to LICENSE file
- [ ] **Config files at root**: brief note explaining why build tool configs live at the repo root

**Verification:**
- [ ] All links resolve (internal docs + screenshots)
- [ ] README renders correctly on GitHub (check headings, images, code blocks)
- [ ] No outdated information — features match what's actually shipped
- [ ] Screenshot images load correctly

**Dependencies:** T1 (correct doc paths), T4 (screenshots exist)

**Files likely touched:**
- `README.md` (major rewrite)

**Estimated scope:** M (writing + verification)

---

## Final Checkpoint: v1.5 Ship 🛑

- [ ] `SPEC.md` at `docs/SPEC.md` with all references updated
- [ ] `docs/README.md` index exists and all links work
- [ ] `.gitignore` verified comprehensive
- [ ] Screenshots in `docs/screenshots/` are current and render
- [ ] `README.md` is comprehensive, accurate, and renders correctly on GitHub
- [ ] No application code changes in this release
- [ ] `npm run build` green — app unchanged
- [ ] `npm run test:ci` green — no test regressions
- [ ] 🛑 **Human review and sign-off before merge**

---

## Summary

| Task | Feature | Scope | Dependencies |
|------|---------|-------|-------------|
| T1 | F16 — move SPEC.md + update refs | S | None |
| T2 | F16 — docs/README.md index | XS | None |
| T3 | F16 — verify .gitignore | XS | None |
| T4 | F15 — capture screenshots | S | None (app state) |
| T5 | F15 — README rewrite | M | T1, T4 |

**Total: 5 tasks across 2 phases + 2 checkpoints.**
